--- a/include/linux/btf.h
+++ b/include/linux/btf.h
@@ -1,5 +1,7 @@
 /* SPDX-License-Identifier: GPL-2.0 */
 /* Copyright (c) 2018 Facebook */
+#include <linux/seq_file.h>
+#include <linux/bpf.h>
 #ifndef _LINUX_BTF_H
 #define _LINUX_BTF_H 1
 
@@ -7,3 +9,20 @@
 
 /* Ensure BBG's __bpf_kfunc is available */
 #ifndef __bpf_kfunc
 #define __bpf_kfunc
 #endif

--- a/net/ipv4/tcp_bbr.c.in
+++ b/net/ipv4/tcp_bbr.c
@@ -18,6 +18,25 @@
 #include <net/pkt_sched.h>
 #include <net/tcp.h>
 
+/* === Linux 5.10 Compatibility Macros === */
+#ifndef GSO_LEGACY_MAX_SIZE
+#define GSO_LEGACY_MAX_SIZE GSO_MAX_SIZE
+#endif
+
+#ifndef __bpf_kfunc
+#define __bpf_kfunc
+#endif
+
+#ifndef __kfunc
+#define __kfunc
+#endif
+
+#ifndef get_random_u32_below
+#define get_random_u32_below(limit) \
+	((limit) ? (get_random_u32() % (limit)) : 0)
+#endif
+
 /* BBR (Bottleneck Bandwidth and RTT) congestion control algorithm.
  *
  * BBR provides robust throughput and low latency for network applications.
